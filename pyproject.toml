[project]
name = "profibus-debug"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.4.1",
    "pyprofibus>=1.13",
    "pyserial>=3.5",
    "rich>=15.0.0",
]

[project.scripts]
profibus-debug = "main:cli"
